spring:
  datasource:
    generate-unique-name: false
    name: tacocloud
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  application:
    name: taco-cloud
  h2:
    console:
      enabled: true 
      path: /h2-console 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect